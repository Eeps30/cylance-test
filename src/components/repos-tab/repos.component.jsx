import React from 'react';
import Container from 'react-bootstrap/Container';
import RepoRows from '../repo-row/repo.rows.component';

const Repos = () => {
  return (
    <Container>
      <RepoRows />
    </Container>
  );
};

export default Repos;
